$(document).ready(function () {
    let amenities = [];
    let checkboxId = $('#checkbox-id');

    if (checkboxId.checked) {
        console.log('done');
    }
    // $('input').change(() => {
    //     if (checkboxId.checked) {
    //         console.log('done');
    //     }
    // });
});